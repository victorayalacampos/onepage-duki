function scrolll(){
    var left = document.querySelector("#slider-eps");
    left.scrollBy(300, 0)
 
}
function scrollr() {
    var right = document.querySelector("#slider-eps");
    right.scrollBy(-300, 0)
}